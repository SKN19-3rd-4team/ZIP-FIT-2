1. 확인사항 전달.
[x]chat 페이지: 추천 질문 클릭 → "답변을 생성하고 있습니다..." 표시되는지
[x]list 페이지: "AI 상담하기" 클릭 → "'[공고 제목]' 문서를 참조 중입니다..." 표시되는지
[x]list 페이지: 공고 예정 카드에 D-day가 표시되지 않는지
[x]list 페이지: 필터 적용 후 2페이지로 넘어가도 정렬 순서 유지되는지
[문제 있음]list 페이지: 공고 카드가 1개 또는 홀수개일 때 레이아웃이 자연스러운지

1.1. 여전히 공고 리스트에서 공고 카드가 하나만 페이지에 출력 될 시, 카드 레이아웃이 전체에 보여집니다.
예를 들어서 지금은 2열 5행으로 각 페이지에서 카드를 보여줌.
마지막 페이지에서 카드가 1개인 경우 1행 1열에 들어가야하는데, 1행 1,2열을 모두 차지하고 있음.

2. 

문제 2 - 채팅 히스토리 로드: 현재 mockData는 준비되어 있으나, 실제 화면 출력이 안 되는 문제는 API 응답 구조와 관련이 있을 수 있습니다. 테스트 후 결과를 알려주시면 수정하겠습니다.
-> 아직 해결 되지 않음.

문제 6 - API 호환성: D-day 계산, 상태 판별 등의 함수들이 실제 API 데이터 구조를 고려하여 작성되었습니다. 실제 API 연동 시 추가 조정이 필요할 수 있습니다.
-> 어떤 추가 조정이 필요한가요? 실제 API 연동해서 호출되고 해당 데이터를 받아서 연결하는데 문제가 있나요?
mockData.js  에서 아래 코드만 수정하면 바로 연결되는 것이 아닌가요?

const USE_MOCK_DATA = false; -> 이 세팅만 하면 되는 것이 아닌지 파악 필요.
```javascript
// ============================================
// 개발 모드 설정
// ============================================
const USE_MOCK_DATA = true; // false로 변경하면 실제 API 사용

// ============================================
// 통합 API 함수 (Mock 또는 실제 API 자동 선택)
// ============================================
```