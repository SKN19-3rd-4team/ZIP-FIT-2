1. merge 후 보고
1.1 api 호출 웹페이지
- main : stat 숫자 데이터 정상
- 네비게이션 정상 : 히스토리 채팅 정상 : 내용 불러오기 정상
- chat 페이지 정상
-> 확인할 요소 : AI 상담 새대화시 로딩 메시지 문제 없는지 / 공고 카드 'AI 상담하기' 진행시 '공고문서'를 참조중입니다. 라고 아까 두 유형이 다르게 로딩되는 부분이 잘 코드로 구성되어있는지 파악.
mockData 에서 진행한 것처럼 일반 -> '답변을 생성하고 있습니다'
공고카드에서 들어가서 채팅 하는 경우 -> '공고이름'문서를 참조 중입니다...

- list 공고 목록 문제 없음 : DB에서 잘 불러와짐.
-> 확인할 요소 : D-day 함수 적용, 각종 태그 추출 적용,
-> 정렬 로직 잘 되어있는지 체크 필요
-> 홀수 공고시 카드 배열 위치 제대로 되어있는지 체크 필요.
-> list 페이지에서 문제되는 부분은 아래와 같다.
필터(전체 기관 | 전체 유형 | 전체 상태) -> 동작하지 않음. 

2. 해결되지 않은 문제.
landing page 에서 서비스 시작할때 다음 페이지가 'user-info' 페이지다.
여전히 이전 사용자 입력 데이터가 입력되어있음.
최초 user-info 들어갈 시는 값이 입력되어있으면 안됨.

3. mockData로 웹페이지 확인하기
main 페이지에서 mockData 로 만든 채팅 히스토리 다 나오지 않음 2개만 리스트 보임.
[o]수원 신혼부부 추천 분양
[o]공고 비교 문의
[x]청년 주택 공고 문의 -> 안 보임.
chat 페이지에서는 '청년 주택 공고 문의' 네비게이션에서 보임.
list 페이지에서는 '청년 주택 공고 문의' 네비게이션에서 보이지 않음.
