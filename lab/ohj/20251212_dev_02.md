1. zf_django 에서 python manage.py runserver 8000
으로 실행시 제대로 접속되는 것을 확인했습니다.
2. main 페이지에서 실제 zf_db 에 등록된 31개의 공고가 올바르게 카운팅되어 stat 에 반영된 것을 확인했습니다.
2.1. main 페이지에서 stat 중에 '이번 주 신규' 는 올바른 카운팅이 아니고 mockData 가 표기 되는 것 같습니다.
3. session_id를 물고 채팅 히스토리 등 올바르게 물고 있는 것을 확인했습니다.
4. chat 페이지가 기존과 동일하게 잘 구성되어있습니다.
4.1. chat에서 실제 api를 통해서 답변이 생성되는 것을 확인했습니다.
4.2. 일반 채팅인 경우는 답변 생성 중입니다 라고 나오는 로딩 문구도 확인했습니다.
4.3. 공고에서 시작하는 ai 상담하기 버튼으로 들어가는 채팅의 경우 채팅을 주고 받으면, '공고이름'문서를 확인하여 답변 생성 중이라는 '로딩 문구' 올바르게 확인했습니다.
5. list 페이지에 실제 데이터 31개가 올바르게 들어온 것을 확인했습니다. 이전에 개발된 다른 사항들도 올바르게 작동되는 것을 확인했습니다.

추가로 확인할 사항
1. views의 파일에 내용들은 docs에 api.yaml 파일에 맞게 끔 나와야되는데 혹시 그것도 확인 가능한가요?

업데이트가 되어야하는 부분
1. list 페이지에서 '필터' 관련된 부분
1.1 현재 상태: 필터 3개 -> 전체 기관| 전체유형| 전체 상태 (옵션: 공고중| 접수중| 마감| 공고예정)
변경 요청: 전체 유형(옵션 : 분양| 임대) | 지역 | 전체 상태 (옵션: 공고중|접수중| 마감| 정정공고중)
- 변경 요청시 전달사항, 기존 '공고예정'은 현재는 기능적 사용을 하지 않지만 나중에 필요로 사용될 수도 있으므로 아예 삭제하지는 않았으면 좋겠다.
- 웹페이지에서는 보이지는 않지만 코드로는 가지고 있으면 좋겠다.
- 마찬가지로 전체 기관 필터링 기능이 웹에서 보이지는 않지만 나중에 사용가능하게 끔해주면 좋겠다.
- 전체 유형도 마찬가지로 현재 상태 : 영구임대주택 | 행복주택 | 분양주택| 전세임대주택| 매입임대주택|국민임대주택| 통합공공임대주택|
으로 되어있는데 현재는 기능적 사용은 안하고 웹페이지에서는 보이지 않지만, 나중에 해당 필터 기능을 사용할 수 있게 남겨주면 좋겠다.

-> 분양| 임대| 로 나누는 것 또한 마찬가지로 @lab\ohj\LIST_31.json 실제 데이터를 참고하면 'annc_type' 의 값을 따르면 될 것 같습니다.
LIST_31.json은 현재 제가 크롤링해서 저장한 db 의 내용입니다. 다만, 우리는 api 호출을 통해서 위 'annc_type' 값을 가져오면 될 것 같습니다.
@lab\ohj\각 API 호출 결과.md -> 새롭게 업데이트 된 팀원의 코드들을 붙여서 확인한 결과입니다. 이 데이터를 참조하세요.

-> '지역' annc_region 을 바탕으로 진행하면 됩니다. 다만, 현재 우리는 31개의 실제 데이터만을 크롤링해서 db에 저장했습니다. 실제로는 더 많은 데이터가 있을 예정입니다.
그러므로 추가적으로 데이터를 더 불러왔을때 '해당 값'을 기준으로 '지역' 필터가 자동적으로 생기면 좋겠습니다. 예를 들면, 현재 31개의 데이터에서 없는 지역에 해당하는 값이 추후에
들어오는 데이터에는 있을 수도 있습니다. 가정해서 '부산광역시' 라는 값이 들어오는 공고인 경우 해당 값을 따라서 '필터'를 자동으로 만들면 좋겠습니다.

2. 위 사항에 맞춰서 각 공고에 대한 메타데이터를 정리해야한다.

2.1 list 페이지에서 '공고 카드'에 관한 부분
2.2. 태그 관련 -> 상태 태그는 1.1에서 수정하는 내용으로 반영할 것.
- 현재 상태 : 기관태그 | 유형태그| 상태태그|
- 변경 요청 : 기관태그 | 지역태그| 상태태그|

2.3. 상태태그 -> 설정
변경사항 및 수정 요청 사항 : @lab\ohj\LIST_31.json 실제 우리 DB 형식을 참조해야합니다.
우리가 기존에는 접수일자가 없기 때문에 임의로 설정해왔습니다. 실제로는 정확하게는 접수일 시작일, 종료일을 통해서 '상태'태그에 대해서 설정하면 됩니다.
하지만 그러기 위해서는 크롤링.ipynb 를 url 에서 공고 상세를 들어가서 안에 내용을 다시 파싱하고 저장해야하기 때문에 기존 이미 만들어진 '크롤링.ipynb' 를 대거 수정해야합니다.
그렇기 때문에 현재는 이미 크롤링해서 데이터를 받은 규격인 @lab\ohj\LIST_31.json 를 참조해서 확인하면, annc_status 라는 컬럼명에 해당하는 값을 따르면 될 것 같습니다.

2.3. @lab\ohj\각 API 호출 결과.md -> 새롭게 업데이트 된 팀원의 코드들을 붙여서 확인한 결과입니다. 이 데이터를 참조하세요.
'모집 기관' -> "annc_pblsh_dt" - "annc_deadline_dt" 데이터 사용할 것.