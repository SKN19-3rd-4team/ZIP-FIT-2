# 개발 시작 가이드

> **모든 확인사항 완료, 개발 시작 준비 완료 ✅**

---

## 📐 프로젝트 구조 및 최종 목표

### 프로젝트 폴더 구조

**현재 개발 구조**:
```
ZIP-FIT-2/
├── figma14/          # 원본 프론트엔드 프로토타입 (HTML, CSS, JS, Bootstrap)
├── zipfit_django/   # 참고용 Django 프로젝트 (구조 참고)
├── zf_django/        # 팀 최신 Django 프로젝트 (최종 병합 대상)
└── figma_django/     # 새로 개발할 프로젝트 (현재 작업 폴더)
```

**참고**: 
- 현재는 폴더 구조가 정리되지 않은 상태입니다.
- 개발 완료 후 문제가 없으면 폴더 구조를 정리할 예정입니다.
- 최종 목표는 `figma_django` 개발 완료 후 `zf_django` 폴더에 병합(merge)하는 것입니다.

### 최종 목표

1. **개발 단계**: `figma_django` 폴더에서 개발 진행
2. **테스트 단계**: 개발 완료 후 검증 및 테스트
3. **병합 단계**: `zf_django` 폴더에 최종 병합

---

## ✅ 기술 결정 사항 (최종 확정)

> **중요**: 아래 기술 결정 사항들은 팀 논의를 통해 확정된 내용입니다. 개발 시 반드시 이 방향을 따릅니다.

### 1. API 서버 주소

**결정 사항**:
- **개발 환경**: `http://localhost:8000`
- **프로덕션**: 아직 없음 (추후 설정)
- **설명**: Django 서버(`manage.py runserver`) 하나로 웹 페이지와 API 모두 제공

**구현 방법**:
```python
# config/settings.py
API_BASE_URL = 'http://localhost:8000'
```

---

### 2. 사용자 ID 생성 방식

**결정 사항**: **25개 형용사 × 25개 동물 = 625가지 조합 방식**

**논의 과정**:
- 옵션 1: 적은 조합 (10개 × 10개) → 다양성 부족
- 옵션 2: 타임스탬프 추가 → 복잡함
- 옵션 3: 랜덤 숫자 추가 → 가독성 저하
- **최종 결정**: 옵션 1 개선 (25개 × 25개) → **625가지 조합, 충분한 다양성, 읽기 쉬움**

**구현 위치**: `web/utils.py`의 `generate_user_id()` 함수

**예시**: `매콤한 숫사슴`, `달콤한 오로라`, `상쾌한 팬더` 등

---

### 3. 세션 키 관리 방식

**결정 사항**: **Django 세션에 저장 (서버 측 관리)**

**논의 과정**:
- 옵션 A: Django 세션에 저장 (서버 측) → **최종 결정**
- 옵션 B: JavaScript sessionStorage에 저장 (클라이언트 측) → 미채택

**선택 이유**:
- 서버에서 관리하므로 더 안전함
- 세션 만료 시 자동 정리됨
- 팀 개발에 문제 없음 (각 개발자 로컬 서버에서 독립적으로 동작)
- `zf_django` 프로젝트와 유사한 방식

**구현 방법**:
```python
# web/utils.py
import uuid

def get_or_create_session_key(request):
    if 'chat_session_key' not in request.session:
        request.session['chat_session_key'] = str(uuid.uuid4())
    return request.session['chat_session_key']
```

**생성 방식**: `uuid.uuid4()` 사용 (예: `8a7e0d3c-9b1f-4d2a-8c5e-6f4b3a2d1e0f`)

---

### 4. 채팅 추천 질문 처리 방식

**결정 사항**: **하드코딩 유지 (프론트엔드에 고정)**

**논의 과정**:
- 옵션 A: 하드코딩 유지 (프론트엔드) → **최종 결정**
- 옵션 B: API에서 동적으로 받아오기 → 미채택

**선택 이유**:
- 간단하고 빠른 구현
- API 엔드포인트 추가 불필요
- 추천 질문은 고정된 4개로 충분

**구현 방법**:
- `chat.html`에 추천 질문 카드 하드코딩
- 클릭 시 `/api/chat` API 호출하여 실제 응답 받기

---

### 5. 채팅 응답 저장 방식

**결정 사항**: **Django 세션 저장 + 개발용 JSON 파일 저장**

**논의 과정**:
- 옵션 A: Django 세션에 저장 → **프로덕션용**
- 옵션 B: 로컬 스토리지에 저장 → 미채택
- 옵션 C: 개발용 JSON 파일 저장 → **개발용**

**최종 결정**: **옵션 A + 옵션 C 조합**
- 프로덕션: Django 세션에 저장 (사용자별 자동 관리)
- 개발 중: 추가로 JSON 파일로 저장 (`lab/ohj/chat_responses/`)하여 응답 형식 확인 및 디버깅

**구현 방법**:
```python
# views.py
def chat_api_view(request):
    # API 호출
    response_data = call_chat_api(...)
    
    # 프로덕션: 세션에 저장
    chat_history = request.session.get('chat_history', [])
    chat_history.append({
        'user_message': user_message,
        'ai_response': response_data,
        'timestamp': datetime.now().isoformat()
    })
    request.session['chat_history'] = chat_history
    
    # 개발용: JSON 파일 저장 (DEBUG 모드일 때만)
    if settings.DEBUG:
        save_response_for_dev(response_data)
    
    return JsonResponse(response_data)
```

---

### 6. 공고 비교 테이블 처리 방식

**결정 사항**: **프론트엔드에서 처리**

**논의 과정**:
- 옵션 A: API에서 비교 형식으로 응답 → 미채택 (API 응답 형식 불확실)
- 옵션 B: 프론트엔드에서 처리 → **최종 결정**

**선택 이유**:
- API 응답 형식에 의존하지 않음
- 프론트엔드에서 자유롭게 디자인 가능
- Bootstrap 테이블 스타일 적용 쉬움
- 더 유연하고 확장 가능

**구현 방법**:
- API 응답에 공고 정보가 2개 이상 포함되어 있을 때
- JavaScript에서 자동으로 비교 테이블 생성
- Bootstrap 테이블 컴포넌트 활용

**조건**: API 응답의 `announcements` 배열 길이가 2개 이상일 때만 테이블 생성

---

### 7. 채팅 응답 형식

**결정 사항**: **구조화된 JSON 형식 (마크다운 텍스트 + 별도 데이터 구조)**

**논의 과정**:
- 가정 1: 구조화된 JSON (마크다운 + 별도 데이터) → **최종 결정**
- 가정 2: HTML 형식 → 미채택

**선택 이유**:
- 프론트엔드에서 렌더링 제어 가능
- 확장성 좋음
- 마크다운 텍스트 + 참고 문서, 공고 카드 등 별도 데이터 구조로 분리

**예상 응답 형식**:
```json
{
    "status": "success",
    "data": {
        "ai_response": {
            "message": "신혼부부 특별공급에 대해 안내드립니다...",
            "message_type": "bot",
            "references": [...],
            "announcements": [...]
        }
    }
}
```

---

### 8. 공고 요약 답변 형식

**결정 사항**: **카드 형식 (구조화된 카드)**

**논의 과정**:
- 옵션 A: 텍스트 요약 → 미채택
- 옵션 B: 카드 형식 → **최종 결정**

**선택 이유**:
- 시각적으로 보기 좋음
- 공고 정보를 한눈에 파악 가능
- `lab/ohj/chat_form/` 예시 파일의 형식이 좋아 보임

**구현 방법**: API 응답의 `announcements` 배열을 카드 형태로 렌더링

---

## 📚 Django runserver 설명

**질문**: "Django에서 manage.py runserver를 해서 실행하는 것 아닌가요?"

**답변**: **맞습니다!** 정확히 그렇게 실행합니다.

### Django 서버 실행 방법

```bash
# figma_django 폴더로 이동
cd figma_django

# Django 서버 실행
python manage.py runserver

# 또는 특정 포트 지정
python manage.py runserver 8000
```

### 실행 후 확인

서버가 실행되면:
```
Starting development server at http://127.0.0.1:8000/
Quit the server with CTRL-BREAK.
```

**브라우저에서 접속**:
- `http://localhost:8000` 또는 `http://127.0.0.1:8000`

### API 서버와의 관계

**중요한 이해**:
- Django 서버 (`manage.py runserver`) = **웹 서버** (프론트엔드 + 백엔드)
- API 엔드포인트 (`/api/chat`) = Django 서버 안의 **하나의 경로**

**예시**:
```
Django 서버 (localhost:8000)
├── / (홈 페이지)
├── /landing (랜딩 페이지)
├── /chat (채팅 페이지)
├── /api/chat (채팅 API) ← 이게 API 엔드포인트!
└── /api/anncs (공고 목록 API)
```

**결론**:
- Django 서버 하나로 모든 것이 동작합니다
- `API_BASE_URL = 'http://localhost:8000'`이 맞습니다
- 별도의 API 서버가 필요 없습니다 (현재는)

---

## 🛠️ 개발 시작 전 준비

### 0. 환경 설정 (필수)

#### 0.1 가상환경 생성 (Conda)

```bash
# Conda 가상환경 생성 (Python 3.12)
conda create -n zipfit_env python=3.12

# 가상환경 활성화
conda activate zipfit_env

# camelot-py 설치 (팀원이 공유한 의존성)
conda install -c conda-forge camelot-py
```

#### 0.2 PostgreSQL + Docker 설정

**Docker 컨테이너 실행**:
```bash
# PostgreSQL + pgvector Docker 컨테이너 실행
docker run -d `
-e POSTGRES_USER=zf_admin `
-e POSTGRES_PASSWORD=zf_pwd1 `
-e POSTGRES_DB=zf_db `
--name zf_db_container `
-p 5432:5432 `
pgvector/pgvector:pg17
```

**Docker 내부 접속 및 확장 설치**:
```bash
# Docker 컨테이너 내부 접속
docker exec -it zf_db_container psql -U zf_admin -d zf_db

# pgvector 확장 설치 (Docker 내부에서 실행)
CREATE EXTENSION IF NOT EXISTS vector;
```

**참고**: 
- 개발 초기에는 SQLite 사용 가능 (PostgreSQL 설정 주석 처리)
- 프로덕션 배포 시 PostgreSQL 필수

#### 0.3 .env 파일 생성

**`figma_django/.env` 파일 생성**:
```env
# Django Secret Key
DJANGO_SECRET_KEY=django-insecure-0#z1rbplff)#c)84_zcg^5ex#p(j)ibh*g%%_dfpai(s9@^tf@

# PostgreSQL Database 설정
DB_HOST=localhost
DB_PORT=5432
DB_USER=zf_admin
DB_PASSWORD=zf_pwd1
DB_NAME=zf_db

# API 설정
API_BASE_URL=http://localhost:8000
```

**주의**: `.env` 파일은 `.gitignore`에 추가하여 Git에 커밋하지 않도록 주의!

#### 0.4 requirements.txt 생성

**`figma_django/requirements.txt` 파일 생성**:
```txt
# Django Core
Django==6.0
djangorestframework==3.16.1

# Database
psycopg2-binary==2.9.11
pgvector==0.4.2

# Environment Variables
python-decouple==3.8

# Utilities
numpy==2.3.5
asgiref==3.11.0
sqlparse==0.5.4
tzdata==2025.2
setuptools==80.9.0
wheel==0.45.1

# HTTP Requests (API 호출용)
requests==2.31.0
```

**의존성 설치**:
```bash
# 가상환경 활성화 후
pip install -r requirements.txt
```

#### 0.5 Django 프로젝트 초기 설정

**프로젝트 생성** (처음 한 번만):
```bash
# figma_django 폴더로 이동
cd figma_django

# Django 프로젝트 생성 (이미 있다면 생략)
django-admin startproject config .

# Django 앱 생성
python manage.py startapp web
python manage.py startapp chatbot
```

**settings.py 설정**:
```python
# config/settings.py
from pathlib import Path
from decouple import config  # 환경 변수 관리

BASE_DIR = Path(__file__).resolve().parent.parent

# 환경 변수에서 읽어오기
SECRET_KEY = config('DJANGO_SECRET_KEY')
DEBUG = True
ALLOWED_HOSTS = ['*']

# INSTALLED_APPS
INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'django.contrib.postgres',  # PostgreSQL 확장
    
    # REST Framework
    'rest_framework',
    
    # 우리의 앱들
    'web',
    'chatbot',
]

# Database 설정
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': config('DB_NAME'),
        'USER': config('DB_USER'),
        'PASSWORD': config('DB_PASSWORD'),
        'HOST': config('DB_HOST'),
        'PORT': config('DB_PORT'),
    }
}

# 또는 개발 초기에는 SQLite 사용 (PostgreSQL 설정 주석 처리)
# DATABASES = {
#     'default': {
#         'ENGINE': 'django.db.backends.sqlite3',
#         'NAME': BASE_DIR / "db.sqlite3",
#     }
# }

# API 설정
API_BASE_URL = config('API_BASE_URL', default='http://localhost:8000')

# Session 설정
SESSION_COOKIE_AGE = 60 * 60 * 24 * 7  # 7일
SESSION_SAVE_EVERY_REQUEST = True
```

**마이그레이션 실행**:
```bash
# 마이그레이션 생성 (필요시)
python manage.py makemigrations

# 마이그레이션 적용
python manage.py migrate

# 관리자 계정 생성 (선택사항)
python manage.py createsuperuser
```

**주의**: PostgreSQL 사용 시 마이그레이션 전에 반드시 `CREATE EXTENSION IF NOT EXISTS vector;` 실행 필요!

---

### 1. 프로젝트 구조 확인

**현재 개발 구조** (위의 "프로젝트 폴더 구조" 섹션 참고):
```
ZIP-FIT-2/
├── figma14/          # 원본 프론트엔드 프로토타입
├── zipfit_django/   # 참고용 Django 프로젝트
├── zf_django/        # 팀 최신 Django 프로젝트 (최종 병합 대상)
└── figma_django/     # 새로 개발할 프로젝트 (현재 작업 폴더)
```

**figma_django 내부 구조**:
```
figma_django/
├── .env              # 환경 변수 (Git에 커밋하지 않음!)
├── requirements.txt
├── manage.py
├── config/
│   ├── settings.py
│   └── urls.py
├── web/
│   ├── utils.py      # 새로 생성 (사용자 ID, 세션 키 생성 함수)
│   ├── views.py
│   ├── urls.py
│   ├── templates/
│   │   ├── base.html
│   │   └── web/
│   │       ├── landing.html
│   │       ├── user_info.html
│   │       ├── main.html
│   │       ├── chat.html
│   │       └── list.html
│   └── static/       # CSS, JS 파일
└── chatbot/
    ├── urls.py
    └── views.py
```

**참고**: 
- 폴더 구조 정리는 개발 완료 후 진행 예정
- 최종 목표는 `zf_django`에 병합하는 것입니다

### 2. 필요한 파일 준비

#### `figma_django/config/settings.py`
```python
# API 설정 추가
API_BASE_URL = 'http://localhost:8000'
```

#### `figma_django/web/utils.py` (새로 생성)
```python
import random
import uuid

def generate_user_id():
    """랜덤 사용자 ID 생성 (625가지 조합)"""
    adjectives = [
        '매콤한', '달콤한', '상쾌한', '용감한', '귀여운', 
        '똑똑한', '빠른', '차분한', '명랑한', '활발한',
        '조용한', '친절한', '멋진', '훌륭한', '당당한',
        '부지런한', '성실한', '밝은', '따뜻한', '시원한',
        '깔끔한', '화사한', '고요한', '신나는', '편안한'
    ]
    
    animals = [
        '숫사슴', '오로라', '팬더', '코알라', '다람쥐',
        '토끼', '고양이', '강아지', '여우', '사자',
        '호랑이', '펭귄', '돌고래', '부엉이', '독수리',
        '곰', '늑대', '사슴', '기린', '코끼리',
        '햄스터', '앵무새', '고슴도치', '수달', '표범'
    ]
    
    return f"{random.choice(adjectives)} {random.choice(animals)}"

def get_or_create_session_key(request):
    """세션 키 가져오기 또는 생성"""
    if 'chat_session_key' not in request.session:
        request.session['chat_session_key'] = str(uuid.uuid4())
    return request.session['chat_session_key']

def get_or_create_user_id(request):
    """사용자 ID 가져오기 또는 생성"""
    if 'user_id' not in request.session:
        request.session['user_id'] = generate_user_id()
    return request.session['user_id']
```

### 3. 개발용 JSON 저장 폴더 생성

```bash
mkdir -p lab/ohj/chat_responses
```

---

## 🎯 개발 단계별 계획

### Phase 1: 기본 구조 설정 ✅ 준비 완료

1. `figma_django` 폴더 생성
2. Django 프로젝트 구조 복사 (`zipfit_django` 기준)
3. 5개 페이지 템플릿 생성 (기본 레이아웃만)
4. `web/utils.py` 생성 및 함수 추가

### Phase 2: 정적 페이지 구현

1. `landing.html` 변환 (데이터 연동 없음)
2. `user_info.html` 변환 (세션 저장만)
3. CSS/JS 파일 이동 및 연결

### Phase 3: API 연동 (Mock 데이터)

1. `main.html` 통계 API 연동 (Mock 응답)
2. `list.html` 공고 목록 API 연동 (Mock 응답)
3. `chat.html` 채팅 API 연동 (Mock 응답)

### Phase 4: 실제 API 연동

1. 실제 API 엔드포인트 연결
2. 응답 형식에 맞춰 렌더링 로직 구현
3. 에러 처리 및 로딩 상태 추가

### Phase 5: 고급 기능

1. 채팅 히스토리 표시
2. 공고 비교 테이블 (프론트엔드 처리)
3. 공고 요약 카드
4. 반응형 디자인 최종 점검

---

## 📝 개발 시 주의사항

### 1. 세션 관리

**Django 세션 사용 시**:
- 세션은 브라우저 쿠키로 관리됨
- 같은 브라우저에서만 유지됨
- 브라우저를 닫으면 세션 만료 (기본 설정)

**팀 개발에서 문제 없는 이유**:
- 각 개발자가 자신의 로컬 서버에서 작업
- 세션이 서로 겹치지 않음
- 프로덕션 배포 시에도 Django 세션은 표준 방식

### 2. API 호출 방식

**프론트엔드에서 직접 호출**:
```javascript
// JavaScript에서 직접 API 호출
fetch('/api/chat', {
    method: 'POST',
    headers: {
        'Content-Type': 'application/json',
        'X-CSRFToken': getCsrfToken()
    },
    body: JSON.stringify({
        user_key: userKey,
        session_key: sessionKey,
        user_message: message
    })
})
```

**또는 Django views를 통한 중계**:
```python
# views.py에서 API 호출 후 프론트엔드에 전달
def chat_api_view(request):
    # Django views에서 API 호출
    response = requests.post(...)
    return JsonResponse(response.json())
```

**제안**: **프론트엔드에서 직접 호출** 추천
- 더 간단하고 빠름
- Django views 부하 감소

### 3. 개발용 JSON 파일 저장

**개발 중에만 사용**:
```python
# views.py 또는 utils.py
import json
from datetime import datetime
import os

def save_response_for_dev(response_data):
    """개발용: API 응답을 JSON 파일로 저장"""
    if not settings.DEBUG:  # 프로덕션에서는 저장 안 함
        return
    
    os.makedirs('lab/ohj/chat_responses', exist_ok=True)
    filename = f"lab/ohj/chat_responses/response_{datetime.now().strftime('%Y%m%d_%H%M%S')}.json"
    
    with open(filename, 'w', encoding='utf-8') as f:
        json.dump(response_data, f, ensure_ascii=False, indent=2)
```

---

## 🚀 개발 시작 명령어

### 1. 초기 설정 (처음 한 번만)

```bash
# 1. 가상환경 생성 및 활성화
conda create -n zipfit_env python=3.12
conda activate zipfit_env
conda install -c conda-forge camelot-py

# 2. 프로젝트 폴더 생성 및 이동
mkdir figma_django
cd figma_django

# 3. zipfit_django 구조 복사 (수동 또는 스크립트)
# 참고: zipfit_django 폴더의 구조를 참고하여 동일하게 생성

# 4. .env 파일 생성 (위의 .env 파일 내용 복사)

# 5. requirements.txt 생성 및 의존성 설치
pip install -r requirements.txt

# 6. Django 프로젝트 생성 (이미 있다면 생략)
django-admin startproject config .
python manage.py startapp web
python manage.py startapp chatbot

# 7. settings.py 설정 (위의 설정 내용 참고)

# 8. 마이그레이션 실행
python manage.py migrate
```

### 2. 개발 서버 실행

```bash
# 가상환경 활성화
conda activate zipfit_env

# 프로젝트 폴더로 이동
cd figma_django

# Django 서버 실행
python manage.py runserver

# 또는 특정 포트 지정
python manage.py runserver 8000
```

### 3. 브라우저에서 확인

```
http://localhost:8000
```

### 4. 개발 중 자주 사용하는 명령어

```bash
# 마이그레이션 생성
python manage.py makemigrations

# 마이그레이션 적용
python manage.py migrate

# 정적 파일 수집 (프로덕션 배포 시)
python manage.py collectstatic

# Django 쉘 실행 (디버깅용)
python manage.py shell
```

---

## 📋 체크리스트

### 개발 시작 전
- [x] API 서버 주소 확인 (`http://localhost:8000`)
- [x] 사용자 ID 생성 방식 결정 (625가지 조합)
- [x] 세션 키 관리 방식 결정 (Django 세션)
- [x] 채팅 추천 질문 처리 방법 결정 (하드코딩)
- [x] 공고 비교 테이블 처리 방법 결정 (프론트엔드)
- [ ] Conda 가상환경 생성 (`zipfit_env`)
- [ ] requirements.txt 생성 및 의존성 설치
- [ ] .env 파일 생성 및 설정
- [ ] Docker PostgreSQL 컨테이너 실행 (선택사항)
- [ ] Django 프로젝트 초기 설정 완료

### 개발 중
- [ ] `figma_django` 폴더 생성
- [ ] Django 프로젝트 구조 설정
- [ ] `web/utils.py` 생성 및 함수 추가
- [ ] 5개 페이지 템플릿 생성
- [ ] CSS/JS 파일 연결
- [ ] API 연동 (Mock → 실제)

### 개발 완료 후
- [ ] 개발용 JSON 파일 정리
- [ ] API 응답 형식 문서화
- [ ] 개발 중 발견한 이슈 문서화

---

## 🎓 초심자를 위한 추가 설명

### Django 서버가 하는 일

1. **웹 페이지 제공**: HTML 파일을 브라우저에 전달
2. **API 제공**: JSON 데이터를 JavaScript에 전달
3. **세션 관리**: 사용자 정보를 서버에 저장

### API 호출 흐름

```
사용자 브라우저
    ↓ JavaScript에서 fetch() 호출
Django 서버 (localhost:8000)
    ↓ /api/chat 경로로 요청
Django views.py
    ↓ API 호출 (또는 직접 처리)
응답 데이터 (JSON)
    ↓ JavaScript로 전달
화면 업데이트
```

### 세션 관리 흐름

```
사용자가 처음 접속
    ↓
Django가 세션 생성 (쿠키에 저장)
    ↓
사용자 ID, 세션 키 생성
    ↓
브라우저 쿠키에 저장
    ↓
다음 요청 시 쿠키로 세션 확인
```

---

---

## ⚠️ 주의사항 및 트러블슈팅

### 1. PostgreSQL 마이그레이션 에러

**에러**: `psycopg2.errors.UndefinedObject: type "vector" does not exist`

**해결책**:
```bash
# Docker 컨테이너 내부 접속
docker exec -it zf_db_container psql -U zf_admin -d zf_db

# pgvector 확장 설치
CREATE EXTENSION IF NOT EXISTS vector;
```

### 2. .env 파일이 인식되지 않을 때

**확인사항**:
- `.env` 파일이 `figma_django` 폴더 루트에 있는지 확인
- `python-decouple` 패키지가 설치되어 있는지 확인
- `settings.py`에서 `from decouple import config` 임포트 확인

### 3. 가상환경이 활성화되지 않을 때

**Windows PowerShell**:
```powershell
conda activate zipfit_env
```

**Windows CMD**:
```cmd
conda activate zipfit_env
```

**Linux/Mac**:
```bash
conda activate zipfit_env
```

### 4. Docker 컨테이너가 실행되지 않을 때

**컨테이너 확인**:
```bash
docker ps -a
```

**컨테이너 재시작**:
```bash
docker start zf_db_container
```

**컨테이너 삭제 후 재생성**:
```bash
docker rm zf_db_container
# 위의 docker run 명령어 다시 실행
```

### 5. 개발 초기에는 SQLite 사용 권장

PostgreSQL 설정이 복잡할 경우, 개발 초기에는 SQLite를 사용해도 됩니다:

```python
# config/settings.py
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': BASE_DIR / "db.sqlite3",
    }
}
```

**장점**:
- 별도 설정 불필요
- 빠른 개발 시작 가능
- 프로덕션 배포 전에 PostgreSQL로 전환 가능

---

---

## 🎯 개발 완료 후 작업

### 1. 폴더 구조 정리

**현재 상태**: 
- `figma14`, `zipfit_django`, `figma_django` 폴더가 정리되지 않은 상태

**정리 계획**:
- 개발 완료 후 문제가 없으면 폴더 구조를 정리할 예정
- 불필요한 파일 정리 및 백업

### 2. 최종 병합 (Merge)

**최종 목표**: `figma_django` 개발 완료 후 `zf_django` 폴더에 병합

**병합 전 체크리스트**:
- [ ] 모든 기능 테스트 완료
- [ ] 코드 리뷰 완료
- [ ] 문서화 완료
- [ ] 환경 설정 확인
- [ ] 충돌 해결

**병합 방법**:
- Git을 사용한 병합 또는 수동 병합
- 팀원과 협의하여 진행

---

**작성일**: 2025-01-20  
**상태**: 개발 준비 완료 ✅  
**다음 단계**: 환경 설정 완료 후 `figma_django` 폴더 생성 및 개발 시작

