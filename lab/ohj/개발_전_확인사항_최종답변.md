# 개발 전 확인사항 최종 답변 및 추가 설명

> **사용자 답변을 바탕으로 한 전문가 설명 및 개발 가이드**

---

## ✅ 답변 확인 완료 및 추가 설명

### 1. API 엔드포인트 URL 설명

**질문**: "실제 서버 주소가 무엇인지 궁금하다"

**답변**:

#### API_BASE_URL이란?

`API_BASE_URL`은 **백엔드 API 서버의 주소**를 의미합니다.

**예시**:
```python
# settings.py
API_BASE_URL = 'http://localhost:8000'  # 개발 환경 (로컬)
# 또는
API_BASE_URL = 'http://api.zipfit.com'  # 프로덕션 환경 (실제 서버)
```

#### 실제 사용 방법

```python
# views.py에서 사용
from django.conf import settings

def chat_api_view(request):
    api_url = f"{settings.API_BASE_URL}/api/chat"
    # 또는
    api_url = "http://localhost:8000/api/chat"  # 직접 지정
```

**현재 상황**:
- `api.yaml`에 `/api/chat`로 정의되어 있음
- Django 서버와 API 서버가 **같은 서버**라면: `http://localhost:8000/api/chat`
- Django 서버와 API 서버가 **다른 서버**라면: `http://api서버주소/api/chat`

**확인 필요**: 백엔드 팀원에게 물어보기
- "API 서버는 어디서 실행되나요? (localhost:8000인가요?)"
- "Django 서버와 같은 서버인가요, 다른 서버인가요?"

---

### 2. 사용자 ID 생성 로직 개선

**질문**: "리스트에 랜덤으로 선택하는 것이라서 다양성이 없어질 것 같은데 괜찮을까요?"

**답변**: 좋은 지적입니다! 더 다양하게 만들 수 있습니다.

#### 개선된 사용자 ID 생성 방법

**방법 1: 더 많은 조합 (추천)**
```python
import random

def generate_user_id():
    adjectives = [
        '매콤한', '달콤한', '상쾌한', '용감한', '귀여운', 
        '똑똑한', '빠른', '차분한', '명랑한', '활발한',
        '조용한', '친절한', '멋진', '훌륭한', '당당한',
        '부지런한', '성실한', '밝은', '따뜻한', '시원한',
        '따뜻한', '시원한', '깔끔한', '화사한', '고요한'
    ]
    
    animals = [
        '숫사슴', '오로라', '팬더', '코알라', '다람쥐',
        '토끼', '고양이', '강아지', '여우', '사자',
        '호랑이', '펭귄', '돌고래', '부엉이', '독수리',
        '곰', '늑대', '사슴', '기린', '코끼리',
        '햄스터', '앵무새', '고슴도치', '수달', '표범'
    ]
    
    # 조합: 25 × 25 = 625가지 가능!
    return f"{random.choice(adjectives)} {random.choice(animals)}"
```

**방법 2: 타임스탬프 추가 (더욱 다양함)**
```python
import random
from datetime import datetime

def generate_user_id():
    adjectives = ['매콤한', '달콤한', ...]  # 25개
    animals = ['숫사슴', '오로라', ...]    # 25개
    
    # 타임스탬프의 초 단위 숫자 사용
    timestamp = datetime.now().second  # 0~59
    number = timestamp % 10  # 0~9
    
    return f"{random.choice(adjectives)} {animals[number]}"
```

**방법 3: 완전 랜덤 (가장 다양함)**
```python
import random
import string

def generate_user_id():
    adjectives = ['매콤한', '달콤한', ...]  # 25개
    animals = ['숫사슴', '오로라', ...]    # 25개
    
    # 랜덤 숫자 추가 (0~9999)
    random_num = random.randint(0, 9999)
    
    return f"{random.choice(adjectives)} {random.choice(animals)}{random_num}"
```

**제안**: **방법 1 (더 많은 조합)** 추천
- 25개 × 25개 = **625가지 조합** 가능
- 충분히 다양함
- 읽기 쉬움

---

### 3. 세션 키 관리 (zf_django 참고)

**질문**: "zf_django 폴더에 참고할 자료가 있지 않을까요?"

**답변**: 확인했습니다! `zf_django/chatbot/views.py`에서 세션 키 생성 방식을 확인했습니다.

#### zf_django의 세션 키 생성 방식

```python
# zf_django/chatbot/views.py 참고
import uuid

def post(self, request):
    session_id = request.data.get("session_id")
    
    # 세션 ID가 없으면 새로 생성
    if not session_id:
        session_id = str(uuid.uuid4())
```

**설명**:
- `uuid.uuid4()`: 완전히 랜덤한 고유 ID 생성 (예: `8a7e0d3c-9b1f-4d2a-8c5e-6f4b3a2d1e0f`)
- 중복 가능성 거의 없음 (약 340조 분의 1)
- API 요청 시 `session_id`를 보내면 기존 세션 사용, 없으면 새로 생성

#### 우리 프로젝트 적용 방법

**옵션 A: Django 세션에 저장 (추천)**
```python
# views.py
import uuid

def chat_api_view(request):
    # 세션 키 가져오기 또는 생성
    if 'chat_session_key' not in request.session:
        request.session['chat_session_key'] = str(uuid.uuid4())
    
    session_key = request.session['chat_session_key']
    
    # API 호출 시 session_key 전달
    api_response = requests.post(
        f"{settings.API_BASE_URL}/api/chat",
        json={
            "user_key": request.session.get('user_id'),
            "session_key": session_key,
            "user_message": user_message
        }
    )
```

**옵션 B: JavaScript에서 생성 (zf_django 방식)**
```javascript
// JavaScript
function getOrCreateSessionKey() {
    let sessionKey = sessionStorage.getItem('chat_session_key');
    if (!sessionKey) {
        // UUID 생성 (간단한 버전)
        sessionKey = 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
            const r = Math.random() * 16 | 0;
            const v = c == 'x' ? r : (r & 0x3 | 0x8);
            return v.toString(16);
        });
        sessionStorage.setItem('chat_session_key', sessionKey);
    }
    return sessionKey;
}
```

**제안**: **옵션 A (Django 세션)** 추천
- 서버에서 관리하므로 더 안전함
- 세션 만료 시 자동 정리됨
- zf_django와 유사한 방식

---

### 4. 공고 비교 테이블 - 프론트엔드 처리 가능 여부

**질문**: "API에서 비교 형식으로 응답을 주는지는 파악을 못했지만, 프론트엔드에서 처리해도 가능할까요?"

**답변**: **가능합니다!** 프론트엔드에서 처리하는 것이 더 유연합니다.

#### 프론트엔드에서 비교 테이블 생성 방법

**시나리오**: API 응답에 공고 정보가 2개 이상 포함되어 있을 때

```json
{
    "status": "success",
    "data": {
        "ai_response": {
            "message": "두 공고를 비교해드리겠습니다.",
            "announcements": [
                {
                    "id": 1,
                    "title": "고양 삼송지구 청년 행복주택",
                    "agency": "LH",
                    "location": "경기도 고양시 덕양구",
                    "recruitment_period": "2025.01.15 ~ 2025.01.25",
                    "deposit": "1,000만원",
                    "monthly_rent": "20만원",
                    "supply_count": 320
                },
                {
                    "id": 2,
                    "title": "일산 식사지구 행복주택",
                    "agency": "LH",
                    "location": "경기도 고양시 일산동구",
                    "recruitment_period": "2025.01.20 ~ 2025.01.30",
                    "deposit": "1,200만원",
                    "monthly_rent": "23만원",
                    "supply_count": 280
                }
            ]
        }
    }
}
```

**JavaScript로 비교 테이블 생성**:
```javascript
function renderComparisonTable(announcements) {
    if (announcements.length < 2) {
        // 공고가 2개 미만이면 일반 카드로 표시
        return renderAnnouncementCards(announcements);
    }
    
    // 비교 테이블 생성
    const table = document.createElement('table');
    table.className = 'table table-bordered comparison-table';
    
    // 헤더
    const thead = document.createElement('thead');
    thead.innerHTML = `
        <tr>
            <th>구분</th>
            ${announcements.map(a => `<th>${a.title}</th>`).join('')}
        </tr>
    `;
    
    // 본문
    const tbody = document.createElement('tbody');
    const fields = [
        { label: '기관', key: 'agency' },
        { label: '위치', key: 'location' },
        { label: '모집 기간', key: 'recruitment_period' },
        { label: '보증금', key: 'deposit' },
        { label: '월세', key: 'monthly_rent' },
        { label: '공급 호수', key: 'supply_count' }
    ];
    
    fields.forEach(field => {
        const row = document.createElement('tr');
        row.innerHTML = `
            <td><strong>${field.label}</strong></td>
            ${announcements.map(a => `<td>${a[field.key] || '-'}</td>`).join('')}
        `;
        tbody.appendChild(row);
    });
    
    table.appendChild(thead);
    table.appendChild(tbody);
    
    return table;
}
```

**장점**:
- API 응답 형식에 의존하지 않음
- 프론트엔드에서 자유롭게 디자인 가능
- Bootstrap 테이블 스타일 적용 쉬움

**제안**: **프론트엔드에서 처리** 추천
- API는 공고 데이터만 제공
- 프론트엔드에서 비교 테이블 생성
- 더 유연하고 확장 가능

---

## 📋 최종 결정 사항 정리

### ✅ 확정된 사항

1. **API 엔드포인트**: `/api/chat` 방식 사용
2. **채팅 추천 질문**: 하드코딩 유지 (옵션 A)
3. **채팅 응답 저장**: 세션 저장 + 개발용 JSON 파일 저장
4. **채팅 응답 형식**: 구조화된 JSON (마크다운 + 별도 데이터)
5. **공고 요약**: 카드 형식 (옵션 B)
6. **공고 비교**: 프론트엔드에서 처리

### 🔍 추가 확인 필요

1. **API 서버 주소**: 백엔드 팀원에게 확인
   - 같은 서버인가요? (`http://localhost:8000`)
   - 다른 서버인가요? (주소 확인 필요)

2. **사용자 ID 생성**: 방법 1 (더 많은 조합) 사용
   - 25개 형용사 × 25개 동물 = 625가지

3. **세션 키 관리**: Django 세션에 저장 (옵션 A)
   - `uuid.uuid4()` 사용
   - zf_django 방식 참고

---

## 🛠️ 개발 시작 전 준비사항

### 1. API 서버 주소 확인

**백엔드 팀원에게 질문**:
```
Q: API 서버는 어디서 실행되나요?
- Django 서버와 같은 서버인가요? (localhost:8000)
- 다른 서버인가요? (주소 알려주세요)
```

**확인 후 `settings.py`에 추가**:
```python
# config/settings.py
API_BASE_URL = 'http://localhost:8000'  # 또는 실제 서버 주소
```

### 2. 사용자 ID 생성 함수 준비

**파일**: `web/utils.py` (새로 생성)

```python
# web/utils.py
import random

def generate_user_id():
    """랜덤 사용자 ID 생성"""
    adjectives = [
        '매콤한', '달콤한', '상쾌한', '용감한', '귀여운', 
        '똑똑한', '빠른', '차분한', '명랑한', '활발한',
        '조용한', '친절한', '멋진', '훌륭한', '당당한',
        '부지런한', '성실한', '밝은', '따뜻한', '시원한',
        '깔끔한', '화사한', '고요한', '신나는', '편안한'
    ]
    
    animals = [
        '숫사슴', '오로라', '팬더', '코알라', '다람쥐',
        '토끼', '고양이', '강아지', '여우', '사자',
        '호랑이', '펭귄', '돌고래', '부엉이', '독수리',
        '곰', '늑대', '사슴', '기린', '코끼리',
        '햄스터', '앵무새', '고슴도치', '수달', '표범'
    ]
    
    return f"{random.choice(adjectives)} {random.choice(animals)}"
```

### 3. 세션 키 관리 함수 준비

**파일**: `web/utils.py`에 추가

```python
# web/utils.py
import uuid

def get_or_create_session_key(request):
    """세션 키 가져오기 또는 생성"""
    if 'chat_session_key' not in request.session:
        request.session['chat_session_key'] = str(uuid.uuid4())
    return request.session['chat_session_key']

def get_or_create_user_id(request):
    """사용자 ID 가져오기 또는 생성"""
    if 'user_id' not in request.session:
        from .utils import generate_user_id
        request.session['user_id'] = generate_user_id()
    return request.session['user_id']
```

---

## 📝 TODO 리스트 (개발 완료 후)

### 개발 완료 후 정리 필요 사항

- [ ] 개발용 JSON 파일 정리 (`lab/ohj/chat_responses/` 폴더)
- [ ] 개발용 JSON 파일을 바탕으로 API 응답 형식 문서화
- [ ] 실제 API 응답 형식과 예상 형식 비교 문서 작성
- [ ] 개발 중 발견한 이슈 및 해결 방법 문서화

---

## 🎯 다음 단계

1. **백엔드 팀원에게 API 서버 주소 확인**
2. **`figma_django` 폴더 생성 및 기본 구조 설정**
3. **Phase 1부터 순차적으로 개발 시작**

---

**작성일**: 2025-01-20  
**작성자**: 개발팀  
**상태**: 개발 준비 완료 ✅

