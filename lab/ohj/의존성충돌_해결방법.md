# 의존성 충돌 해결 방법

> **pip 설치 시 `ResolutionImpossible` 오류 해결**

---

## 🔍 문제 상황

**오류 메시지**:
```
ERROR: ResolutionImpossible: for help visit https://pip.pypa.io/en/latest/topics/dependency-resolution/#dealing-with-dependency-conflicts
```

**원인**:
- 패키지 간 버전 충돌
- requirements.txt에 지정된 버전과 현재 환경의 패키지 버전 충돌

---

## ✅ 해결 방법

### 방법 1: 개별 패키지 설치 (권장)

**의존성 충돌이 발생하는 패키지를 제외하고 개별 설치**:

```bash
conda activate zipfit_env

# 필수 패키지만 개별 설치
pip install python-dotenv
pip install beautifulsoup4
pip install requests
pip install psycopg2-binary
pip install pandas
pip install numpy

# pgvector는 필요시 별도 설치 (충돌 가능성 높음)
pip install pgvector
```

### 방법 2: 버전 제약 완화

**requirements.txt 없이 최신 버전 설치**:

```bash
pip install python-dotenv beautifulsoup4 requests psycopg2-binary pandas numpy --upgrade
```

### 방법 3: 가상환경 재생성 (최후의 수단)

**깨끗한 환경에서 시작**:

```bash
# 새 가상환경 생성
conda create -n zipfit_crawler python=3.12
conda activate zipfit_crawler

# 필수 패키지 설치
pip install python-dotenv beautifulsoup4 requests psycopg2-binary pandas numpy
```

---

## 📝 크롤링에 필요한 최소 패키지

**반드시 필요한 패키지**:
1. `python-dotenv` - 환경 변수 로드
2. `beautifulsoup4` - HTML 파싱 (bs4)
3. `requests` - HTTP 요청
4. `psycopg2-binary` - PostgreSQL 연결
5. `pandas` - 데이터 처리
6. `numpy` - 수치 연산

**선택적 패키지**:
- `pgvector` - 벡터 데이터베이스 (의존성 충돌 가능성 높음)

---

## 🔧 Cell 2 수정 내용

**자동 설치 로직 개선**:
- 각 패키지를 개별적으로 설치 시도
- 오류 발생해도 계속 진행
- 설치 실패 시 경고만 출력하고 계속 진행

---

## 💡 빠른 해결

**Jupyter Notebook 셀에서 직접 실행**:

```python
import sys
import subprocess

# 필수 패키지만 설치
packages = ['python-dotenv', 'beautifulsoup4', 'requests', 'psycopg2-binary', 'pandas', 'numpy']

for package in packages:
    try:
        subprocess.check_call([sys.executable, "-m", "pip", "install", package, "--quiet"])
        print(f"✅ {package} 설치 완료")
    except:
        print(f"⚠️ {package} 설치 실패 (수동 설치 필요)")
```

---

## ⚠️ 주의 사항

### 1. pgvector 설치
- `pgvector`는 의존성 충돌 가능성이 높음
- 크롤링 단계에서는 필수 아님
- 벡터 검색 기능 사용 시에만 필요

### 2. 버전 충돌
- requirements.txt의 버전이 현재 환경과 맞지 않을 수 있음
- 최신 버전으로 설치하는 것이 안전할 수 있음

### 3. 가상환경 확인
- 올바른 가상환경이 활성화되어 있는지 확인
- Jupyter Notebook이 올바른 커널을 사용하는지 확인

---

**작성일**: 2025-01-20  
**상태**: 의존성 충돌 해결 방법 작성 완료

