# 크롤링 추가 패키지 설치 가이드

> **크롤링 실행 중 필요한 추가 패키지**

---

## 📦 추가로 필요한 패키지

크롤링 노트북 실행 중 발견된 추가 패키지:

1. **llama-cloud-services** - LlamaParse 사용 (PDF 파싱)
2. **langchain-openai** - OpenAI 임베딩 사용

---

## ✅ 자동 설치

**크롤링.ipynb의 Cell 2가 자동으로 설치합니다.**

Cell 2 실행 시:
- `llama-cloud-services` 자동 설치
- `langchain-openai` 자동 설치

---

## 🔍 수동 설치 (필요시)

**터미널에서 실행**:
```bash
conda activate zipfit_env
pip install llama-cloud-services langchain-openai
```

---

## 📝 패키지별 용도

### llama-cloud-services
- **용도**: LlamaParse를 통한 PDF 파싱
- **사용 위치**: `src/parser.py`
- **기능**: PDF 문서를 구조화된 마크다운으로 변환

### langchain-openai
- **용도**: OpenAI 임베딩 모델 사용
- **사용 위치**: `src/embedder.py`
- **기능**: 텍스트를 벡터로 변환 (임베딩)

---

## ⚠️ 주의 사항

### 1. API 키 필요
- `llama-cloud-services`는 LlamaCloud API 키 필요
- `langchain-openai`는 OpenAI API 키 필요
- `.env` 파일에 설정 필요:
  ```env
  OPENAI_API_KEY=your_openai_api_key
  # LlamaCloud API 키도 필요할 수 있음
  ```

### 2. 비용 발생 가능
- LlamaParse API 사용 시 비용 발생 가능
- OpenAI API 사용 시 비용 발생 가능
- 사용량 확인 필요

---

## 💡 빠른 해결

**Jupyter Notebook 셀에서 직접 실행**:
```python
import sys
import subprocess

packages = ['llama-cloud-services', 'langchain-openai']
for package in packages:
    try:
        subprocess.check_call([sys.executable, "-m", "pip", "install", package, "--quiet"])
        print(f"✅ {package} 설치 완료")
    except:
        print(f"⚠️ {package} 설치 실패")
```

---

**작성일**: 2025-01-20  
**상태**: 크롤링 추가 패키지 가이드 작성 완료

