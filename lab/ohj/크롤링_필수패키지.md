# 크롤링 필수 패키지 설치 가이드

> **크롤링 노트북 실행에 필요한 패키지 목록**

---

## 📦 필수 패키지 목록

크롤링 노트북 실행에 필요한 주요 패키지:

1. **python-dotenv** - 환경 변수 로드
2. **beautifulsoup4** - HTML 파싱 (bs4)
3. **requests** - HTTP 요청
4. **psycopg2-binary** - PostgreSQL 연결
5. **pgvector** - 벡터 데이터베이스
6. **pandas** - 데이터 처리
7. **numpy** - 수치 연산

---

## ✅ 자동 설치

**크롤링.ipynb의 Cell 2가 자동으로 설치합니다.**

Cell 2 실행 시:
- 필수 패키지가 없으면 자동으로 설치
- 각 패키지 설치 상태 확인 및 출력

---

## 🔍 수동 설치 (필요시)

**터미널에서 실행**:
```bash
conda activate zipfit_env  # 또는 zf2_back
pip install python-dotenv beautifulsoup4 requests psycopg2-binary pgvector pandas numpy
```

**또는 requirements.txt 사용**:
```bash
cd zf_crawler
pip install -r requirements.txt
```

---

## 📝 패키지별 용도

### python-dotenv
- `.env` 파일에서 환경 변수 로드
- 데이터베이스 연결 정보 등 관리

### beautifulsoup4 (bs4)
- HTML/XML 파싱
- 웹 크롤링 시 HTML 구조 분석

### requests
- HTTP 요청
- 웹 페이지 다운로드

### psycopg2-binary
- PostgreSQL 데이터베이스 연결
- SQL 쿼리 실행

### pgvector
- 벡터 데이터베이스 확장
- 임베딩 벡터 저장 및 검색

### pandas
- 데이터프레임 처리
- 크롤링 데이터 정리 및 변환

### numpy
- 수치 연산
- 벡터 연산 등

---

## ⚠️ 오류 해결

### ModuleNotFoundError: No module named 'bs4'
**해결**: `beautifulsoup4` 패키지 설치 필요
```bash
pip install beautifulsoup4
```

### ModuleNotFoundError: No module named 'dotenv'
**해결**: `python-dotenv` 패키지 설치 필요
```bash
pip install python-dotenv
```

### ModuleNotFoundError: No module named 'psycopg2'
**해결**: `psycopg2-binary` 패키지 설치 필요
```bash
pip install psycopg2-binary
```

---

## 💡 팁

### 1. 한 번에 설치
```bash
pip install python-dotenv beautifulsoup4 requests psycopg2-binary pgvector pandas numpy
```

### 2. requirements.txt 사용
```bash
cd zf_crawler
pip install -r requirements.txt
```

### 3. Jupyter Notebook에서 설치
```python
import sys
import subprocess

packages = ['python-dotenv', 'beautifulsoup4', 'requests', 'psycopg2-binary', 'pgvector', 'pandas', 'numpy']
for package in packages:
    subprocess.check_call([sys.executable, "-m", "pip", "install", package, "-q"])
```

---

**작성일**: 2025-01-20  
**상태**: 크롤링 필수 패키지 가이드 작성 완료

